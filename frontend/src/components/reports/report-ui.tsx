/**
 * The reports KPI card is the shared MetricCard. Re-exported under the historical
 * `KpiCard` name so the reports tabs need no churn.
 */
export { MetricCard as KpiCard } from "../common/MetricCard";
export type { Accent } from "../common/accent";
